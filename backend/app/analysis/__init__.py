"""Pure scoring analytics: aggregation, comparison, regression detection, and slices.

Everything in this package operates on plain records (see ``records``) and has no database or
HTTP dependencies, which keeps the evaluation methodology easy to test and reason about.
"""
